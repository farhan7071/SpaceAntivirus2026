# Sprint 003 baseline — no business logic to keep rules for yet.
# Hilt/Room/Retrofit/kotlinx.serialization each generate their own
# consumer-rules.pro bundled in their AARs, so this file intentionally
# stays close to empty; add feature-specific keep rules only when a
# feature module actually needs one (e.g. serialized model classes).
